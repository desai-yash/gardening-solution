<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gardeningsolution">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GardeningSolution">
        <activity android:name=".date_picker"></activity>
        <activity android:name=".verifymail" />
        <activity android:name=".forgot_passwd" />
        <activity android:name=".signup" />
        <activity android:name=".services" />
        <activity android:name=".profile" />
        <activity android:name=".homepage" />
        <activity android:name=".Login" />
        <activity android:name=".Login_Signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>